import styled from "styled-components";

const Tablist = styled.div`
    position: relative;
    z-index: 3;
    width: max-content;
    padding: 0px;
    margin: 0px;
    list-style: none;
`;

export default Tablist;
